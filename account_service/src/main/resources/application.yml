server:
  port: 8081

spring:
  application:
    name: ACCOUNT-SERVICE

eureka:
  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      defauldZone: http://localhost:8761/eureka/
  instance:
    hostname: localhost